server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/aquashdw2/spring-cloud-config.git
          clone-on-start: true
          basedir: .config-git-remote

  security:
    user:
      name: root
      password: asLK1209